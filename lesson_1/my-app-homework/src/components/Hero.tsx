import React from 'react';

const Hero = () => {
    return (
        <section className="hero" id="hero">
            <div className="container hero__inner">
                <h1 className="hero__title">Шалом, православные!</h1>
                <p className="hero__text">Дорогу осилит идущий!</p>
            </div>
        </section>
    );
};

export default Hero;